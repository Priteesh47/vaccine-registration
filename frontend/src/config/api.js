export const API_URL = 'http://localhost:9000/api';

export const api = {
    profile: `${API_URL}/profile`,
    auth: `${API_URL}/auth`,
    appointments: `${API_URL}/appointments`,
    vaccines: `${API_URL}/vaccines`,
    centers: `${API_URL}/centers`,
    dashboard: `${API_URL}/dashboard`
};

export default api; 